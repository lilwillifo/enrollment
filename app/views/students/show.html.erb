<%= @student.name %>
<%= @addresses.each do |address| %>
  <%= address.description %>
  <%= address.street %>
  <%= address.city %>
  <%= address.state %>
  <%= address.zip %>
<% end %>

<%= @courses.each do |course| %>
  <%= course.title %>
<% end %>
